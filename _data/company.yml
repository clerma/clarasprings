---
name: "Clara Springs Camp and Conference"
logo_image: "/assets/img/img/logo-light.png"
phone: "{{ site.phone | default: '' }}"
email: "{{ site.email | default: '' }}"
website: "{{ site.url }}"
description: "The page you are looking for was moved, removed, renamed or never existed."
address:
  street: "{{ site.address.street | default: '' }}"
  city: "{{ site.address.city | default: '' }}"
  state: "{{ site.address.state | default: '' }}"
  zip: "{{ site.address.zip | default: '' }}"
  country: "{{ site.address.country | default: '' }}"
social:
  facebook: "{{ site.social.facebook | default: '' }}"
  twitter: "{{ site.social.twitter | default: '' }}"
  linkedin: "{{ site.social.linkedin | default: '' }}"
  instagram: "{{ site.social.instagram | default: '' }}"
business_hours:
  monday: "{{ site.business_hours.monday | default: '9:00 AM - 5:00 PM' }}"
  tuesday: "{{ site.business_hours.tuesday | default: '9:00 AM - 5:00 PM' }}"
  wednesday: "{{ site.business_hours.wednesday | default: '9:00 AM - 5:00 PM' }}"
  thursday: "{{ site.business_hours.thursday | default: '9:00 AM - 5:00 PM' }}"
  friday: "{{ site.business_hours.friday | default: '9:00 AM - 5:00 PM' }}"
  saturday: "{{ site.business_hours.saturday | default: 'Closed' }}"
  sunday: "{{ site.business_hours.sunday | default: 'Closed' }}"
